---
title: Homework 3
subtitle: "Data Wrangling with `tidyr`, `forcats`, and `stringr`"
date: last-modified
from: markdown+emoji
execute:
  eval: true
format:
  html:
    code-fold: false
    toc: true
    toc-depth: 5
---

```{r}
#| include: false

library(tidyverse)
library(ggthemes)
#library(hrbrthemes)
library(skimr)
library(kableExtra)
library(DT)
```


# 📌 Directions

- Submit **one Quarto document** (`.qmd`) to Brightspace:

  - `danl-310-hw2-LASTNAME-FIRSTNAME.qmd`  
    (e.g., `danl-310-hw2-choe-byeonghak.qmd`)

- **Due:** **March 9, 2026, 11:59 P.M. (ET)**

- For **visualization questions**, you must provide:
  1) the **`ggplot2` code**, and  
  2) a **written comment** (2–4 sentences) interpreting the corresponding figure.


<br>

# ✅ Setup

```{r}
#| echo: true

library(tidyverse)
library(ggthemes)
#library(hrbrthemes)
library(skimr)
```

---

<br><br>

# Part 1. IMDb Holiday Movies

<div style="text-align:center; width: 50%; margin: auto;">
  <img src="https://bcdanl.github.io/lec_figs/christmas_movies.jpg" alt="" style="max-width: 1000px; width: 100%; border-radius: 0px; box-shadow: 0 0 12px rgba(0,0,0,.15);">
</div>


- The following is the data.frame for Part 1.

```{r}
holiday_movies <- read_csv("https://bcdanl.github.io/data/holiday_movies.csv")
```

- The data.frame **holiday_movies** comes from the Internet Movie Database (IMDb). 


## Variable description for `holiday_movies`

- **`tconst`**: alphanumeric unique identifier of the title

- **`title_type`**: the type/format of the title 
  - (**movie**, **video**, or **tvMovie**)

- **`primary_title`**: the more popular title / the title used by the filmmakers on promotional materials at the point of release

- **`simple_title`**: the title in lowercase, with punctuation removed, for easier filtering and grouping

- **`year`**: the release year of a title

- **`runtime_minutes`**: primary runtime of the title, in minutes

- **`average_rating`**: weighted average of all the individual user ratings on IMDb

- **`num_votes`**: number of votes the title has received on IMDb (titles with fewer than 10 votes were not included in this dataset)

<br>


- The following is the data.frame, **`holiday_movies`**.
```{r}
rmarkdown::paged_table(holiday_movies,
                       options = list(rows.print = 20,
                                      cols.print = 6,
                                      pages.print = 0,
                                      paged.print = F
                                      )) 
```

<br>


- The following is another data.frame **holiday_movie_genres** that is related with the data.frame **holiday_movies**:

```{r}
holiday_movie_genres <- read_csv("https://bcdanl.github.io/data/holiday_movie_genres.csv")
```


```{r}
rmarkdown::paged_table(holiday_movie_genres,
                       options = list(rows.print = 20,
                                      cols.print = 6,
                                      pages.print = 0,
                                      paged.print = F
                                      )) 
```

- The data.frame **holiday_movie_genres** include up to three genres associated with the titles that appear in the data.frame.



## Variable description for `holiday_movie_genres`

  - **tconst**: alphanumeric unique identifier of the title
  - **genres**: genres associated with the title, one row per genre


<br><br>


## Q1a.

- Provide the R code to generate the data.frame, **holiday_movie_with_genres**, which combines the two data.frames, **holiday_movies** and **holiday_movie_genres**:

- The following shows the **holiday_movie_with_genres** data.frame:

```{r}
#| echo: true
holiday_movie_with_genres <- read_csv(
  "https://bcdanl.github.io/data/holiday_movie_w_genres.csv"
)
```

```{r}
rmarkdown::paged_table(holiday_movie_with_genres,
                       options = list(rows.print = 20,
                                      cols.print = 6,
                                      pages.print = 0,
                                      paged.print = F
                                      )) 
```



```{r}
holiday_movie_with_genres <- holiday_movies %>%
  left_join(holiday_movie_genres, by = "tconst")
```


<br><br>

## Q1b.

- Provide the R code to see how the summary statistics---mean, median, standard deviation, minimum, maximum, first and third quartiles---of **average_rating** and **num_votes** varies by popular **genres** and **title_type**.

  - Consider only the five popular **genres**, which are selected in terms of the number of titles for each genre.
  - Removes the **video** type of the titles when calculating the summary statistics.



```{r}
top_genres <- holiday_movie_with_genres %>%
  count(genres, sort = TRUE) %>%
  slice_head(n = 5) %>%
  pull(genres)

holiday_movie_with_genres %>%
  filter(genres %in% top_genres, title_type != "video") %>%
  group_by(genres, title_type) %>%
  summarise(across(c(average_rating, num_votes),
                   list(mean = ~mean(.x, na.rm = TRUE),
                        median = ~median(.x, na.rm = TRUE),
                        sd = ~sd(.x, na.rm = TRUE),
                        min = ~min(.x, na.rm = TRUE),
                        q1 = ~quantile(.x, 0.25, na.rm = TRUE),
                        q3 = ~quantile(.x, 0.75, na.rm = TRUE),
                        max = ~max(.x, na.rm = TRUE))))
```


<br><br>

## Q1c.

- Provide R code to recreate the ggplot figure illustrating how the relationship between **log10(num_votes)** and **average_rating** varies by the popular **genres** and **title_type**.
  - The five popular **genres** are selected in terms of the number of titles for each genre.
  - The **video** type of the titles are removed in the ggplot figure.


![](https://bcdanl.github.io/lec_figs/danl-310-hw3-q1c.png)



```{r}
top_genres <- holiday_movie_with_genres %>%
  count(genres, sort = TRUE) %>%
  slice_head(n = 5) %>%
  pull(genres)

holiday_movie_with_genres %>%
  filter(genres %in% top_genres, title_type != "video") %>%
  ggplot(aes(x = log10(num_votes), y = average_rating, color = genres)) +
  geom_point(alpha = 0.10, color = "black") +
  geom_smooth(method = "lm", se = TRUE) +
  facet_grid(title_type ~ genres) +
  labs(x = "Number of Votes (in base-10 log)",
       y = "Average Rating",
       color = "Genre") +
  theme_fivethirtyeight() +
  theme(
    legend.text = element_text(size = 6),
    legend.title = element_text(size = 6),
    axis.text = element_text(size = 7),
    axis.title.y = element_text(size = 7)
  )
```



<br><br>

## Q1d.

- Provide a comment to illustrate how the relationship between **log10(num_votes)** and **average_rating** varies by the popular **genres** and **title_type**.



```{r}
# The most popular genres are comedy and drama. The average rating seems to decrease in some genres whether they are a movie or tvmovie. 
```

<br><br>

## Q1e.

- Provide R code to recreate the ggplot figure illustrating the annual trend of the share of number of movies by popular genre from year 1975 to 2022.

  - For genres that are not popular, categorize them as **"Other"**.
  - Consider changing the order of categories in **genres**.

![](https://bcdanl.github.io/lec_figs/danl-310-hw3-q1e.png)



```{r}
top_genres <- holiday_movie_with_genres %>%
  count(genres, sort = TRUE) %>%
  slice_head(n = 5) %>%
  pull(genres)

holiday_movie_with_genres %>%
  filter(year >= 1975, year <= 2022) %>%
  mutate(
    genres = ifelse(genres %in% top_genres, genres, "Other"),
    genres = factor(genres, levels = c("Animation","Other","Family","Drama","Romance","Comedy"))
  ) %>%
  ggplot(aes(x = year, fill = genres)) +
  geom_bar(position = "fill") +
  scale_y_continuous(labels = scales::percent) +
  scale_fill_manual(values = c(
    "Animation" = "deeppink",
    "Other" = "blue",
    "Family" = "lightyellow",
    "Drama" = "peachpuff",
    "Romance" = "plum",
    "Comedy" = "lightgreen"
  )) +
  labs(
    title = "How have favorite genres of holiday movies evolved over time?",
    x = "year",
    y = "Share of number of movies by genre",
    fill = "Genre",
    caption = "Source: IMDb"
  ) +
  theme_fivethirtyeight() +
  theme(
    legend.position = "right",
    legend.direction = "vertical",
    plot.title = element_text(size = 7),
    plot.caption = element_text(size = 7),
    axis.title.x = element_text(size = 7),
    axis.title.y = element_text(size = 7)
    
  )
```


<br>

## Q1f.

- Provide a comment to illustrate the annual trend of (1) the share of number of movies by popular genre from year 1975 to 2022.

  - Which genre has become more popular since 2010?



```{r}
# Comedy and family movies have increase in popularity over the years. Comedy has become most popular in 2010. 
```

<br><br>

## Q1g.

- Add the following two variables---**christmas** and **holiday**---to the data.frame **holiday_movies_with_genres**:

- **christmas**: 
  - **TRUE** if the **simple_title** includes "christmas", "xmas", "x mas"
  - **FALSE** otherwise
  
  
- **holiday**: 
  - **TRUE** if the **simple_title** includes "holiday"
  - **FALSE** otherwise



```{r}
holiday_movie_with_genres <- holiday_movie_with_genres %>%
  mutate(
    christmas = str_detect(simple_title, "christmas|xmas|x mas"),
    holiday = str_detect(simple_title, "holiday")
  )
```


<br><br>

## Q1h.

- Provide R code to recreate the ggplot figure illustrating the annual trend of (1) the number of movie titles with "holiday" varies by **christmas**.

![](https://bcdanl.github.io/lec_figs/danl-310-hw3-q1h.png)



```{r}
movie_counts <- holiday_movie_with_genres %>%
  group_by(year, christmas, holiday) %>%
  summarise(n_movies = n(), .groups = "drop")

ggplot(movie_counts, aes(x = year, y = n_movies, color = holiday)) +
  geom_point(alpha = 0.5) +
  geom_smooth(se = TRUE) +
  facet_wrap(~christmas, labeller = as_labeller(c(
    "FALSE" = 'A title does not\ninclude a "christmas".',
    "TRUE"  = 'A title includes\na "christmas".'
  ))) +
  labs(
    x = "Year",
    y = "Number of movies",
    color = 'Does a\ntitle include\na "holiday"?'
  ) +
  theme_bw() +
  theme(
    legend.text = element_text(size = 8),
    legend.title = element_text(size = 9),
    legend.direction = "vertical",
    strip.background = element_rect(fill = "hotpink"),
    strip.text = element_text(face = "italic", color = "black"),
    axis.text.x = element_text(size = 8)
  )
```






<br><br><br>


# Part 2. TripAdvisor

<div style="text-align:center; width: 50%; margin: auto;">
  <img src="https://bcdanl.github.io/lec_figs/tripadvisor.png" alt="" style="max-width: 1000px; width: 100%; border-radius: 0px; box-shadow: 0 0 12px rgba(0,0,0,.15);">
</div>


- The following is the data.frame for Part 2.

```{r}
tripadvisor <- read_csv("https://bcdanl.github.io/data/tripadvisor_cleaned.csv")
```

- TripAdvisor is an online travel research company that empowers people around the world to plan and enjoy the ideal trip. 

-  TripAdvisor wanted to know whether promoting membership on their platform could drive engagement and bookings. 

- To do so, TripAdvisor had just run an experiment to explore user retention by offering a random subset of customers an easier sign-up process for membership.


## Variable description

- **`id`**: a unique identifier for a user.

- **`time`**:	
  - **PRE** if time is before the experiment; 
  - **POST** if time is in the 28 days after the experiment.
  - For each **id** value, there are two observations---one with **time == "PRE"** and the other with **time == "POST"**.
  
  
- **`days_visited`**:	Number of days a user visited the TripAdvisor website.

- **`easier_signup`**: 
  - **TRUE** if a user was exposed to the easier signup process (e.g., one-click signup) during the experiment;
  - **FALSE** otherwise.
  
- **`became_member`**: 
  - **TRUE** if a user became a member during the experiment period;
  - **FALSE** otherwise.
  
- **`locale_en_US`**: 
  - **TRUE** if a user accessed the website from the US;
  - **FALSE** otherwise.

- **`os_type`**:	Windows, Mac, or Others

- **`revenue_pre`**:  Amount of dollars a user spent on the website before the experiment



- The following is the data.frame, **tripadvisor**.
```{r}
rmarkdown::paged_table(tripadvisor,
                       options = list(rows.print = 20,
                                      cols.print = 6,
                                      pages.print = 0,
                                      paged.print = F
                                      )) 
```

<br><br>

## Q2a.

- Using the given data.frame, **tripadvisor**, create the data.frame, **tripadvisor**, for which

  - **time** is a factor-type variable of **time** with the first level, **"PRE"**.
  

```{r}
tripadvisor <- tripadvisor %>%
  mutate(time = factor(time, levels = c("PRE", "POST")))
```


<br><br>

## Q2b.
- Provide R code to recreate the ggplot figure illustrating how the relationship between **time** and **days_visited** varies by **easier_signup** and **became_member**.

  - Row-wise split is based on **easier_signup**.
  - Column-wise split is based on **became_member**.


![](https://bcdanl.github.io/lec_figs/danl-310-hw3-q2b.png)


```{r}
ggplot(tripadvisor, aes(x = time, y = days_visited, fill = time)) +
  geom_boxplot() +
  facet_grid(easier_signup ~ became_member,
             labeller = labeller(
               easier_signup = c(
                 "TRUE" = "A user was exposed\nto the\neasier signup process",
                 "FALSE" = "A user was NOT exposed\nto the easier\nsignup process"
               ),
               became_member = c(
                 "TRUE" = "A user became a member",
                 "FALSE" = "A user did NOT become a member"
               )
             )) +
  scale_fill_manual(values = c("PRE" = "red", "POST" = "steelblue")) +
  labs(
    x = "",
    y = "Number of days visited"
  ) +
  theme_bw() +
  theme(
    legend.position = "none",
    strip.text = element_text(size = 7),
    axis.title = element_text(size = 7),
    axis.text.x = element_text(size = 7)
  )
```



<br><br>

## Q2c.
- Provide a comment to illustrate how the relationship between **time** and **days_visited** varies by **easier_signup** and **became_member**.

*Answer:*
```{r}
# The number of days visited decreases from pre to post across the groups. Users who were exposed to the easier sign up process and became members show a small decline. 
```



<br><br>


## Q2d.
- Provide a R code to create the data.frame **q2d** that includes the variable **diff**, the difference between (1) the value of **days_visited** for time == PRE and (2) the value of **days_visited** for **time == POST** for each **id**.


- The resulting data.frame should look as follows:

```{r}
#| echo: true
q2d <- read_csv('https://bcdanl.github.io/data/tripadvisor_diff.csv')
```

```{r}
rmarkdown::paged_table(q2d,
                       options = list(rows.print = 20,
                                      cols.print = 6,
                                      pages.print = 0,
                                      paged.print = F
                                      )) 
```

- Include only the variables as shown above.
- Adjust the order of the variables as shown above.


```{r}
q2d <- tripadvisor %>%
  select(id, time, days_visited) %>%
  pivot_wider(names_from = time, values_from = days_visited) %>%
  mutate(diff = PRE - POST) %>%
  select(id, diff)
```



<br><br>


## Q2e.

- Provide an R code to calculate how the difference in the mean value of **diff** varies by **easier_signup** and **became_member** using the data.frame **q2d**.

- The resulting data.frame should look as follows:

```{r}
#| echo: true
q2e <- read_csv('https://bcdanl.github.io/data/tripadvisor_diff_mean.csv')
```

```{r}
rmarkdown::paged_table(q2e,
                       options = list(rows.print = 20,
                                      cols.print = 6,
                                      pages.print = 0,
                                      paged.print = F
                                      )) 
```



```{r}
q2e <- q2d %>%
  left_join(tripadvisor %>% select(id, easier_signup, became_member) %>% distinct(),
            by = "id") %>%
  group_by(easier_signup, became_member) %>%
  summarise(mean_diff = mean(diff), .groups = "drop")
```



<br><br>


## Q2f.
- Using the resulting data.frame in Q2e, discuss the following question:
  - What is the effect of easier signup process on the number of days a user visited the TripAdvisor website? 
  - How does this effect varies by the status of **became_member**?

*Answer:*
```{r}
#The easier sign up process has a smaller decline in the number of days visited compared to the people who were not exposed to it. The effect is stronger fro users who became members because their change in visits is smaller than for non-members. Users who did not become members show a large decrease in days visited. 
```




<br><br><br>


# Question 3. Netflix 


<div style="text-align:center; width: 67%; margin: auto;">
  <img src="https://bcdanl.github.io/lec_figs/netflix.png" alt="" style="max-width: 1000px; width: 100%; border-radius: 0px; box-shadow: 0 0 12px rgba(0,0,0,.15);">
</div>



The following is the data.frame for Question 3:

```{r}
#| echo: true
netflix <- read_csv(
  "https://bcdanl.github.io/data/netflix_cleaned.csv")

```

## Variable Description

- **`id`**: unique identifier for each show

- **`title`**: title

- **`description`**: description

- **`director`**: directors

- **`genre`**: genres
  - Each content can belong to multiple genres. 
  - They are seperated by commas.
  - The maximum number of genres a show can belong to is 3.
  
- **`cast`**: cast (seperated by commas)

- **`production_country`**: production countries 
  - Each content could be produced in multiple countries. 
  - They are separated by commas. 
  - The maximum number of countries in which a show was produced is 8.
  
- **`release_date`**: release date (year in which it was released)

- **`date_added`**: date the show was added to Netflix
  -  e.g., the format of the value is character with MONTH, DAY, YEAR

- **`rating`**: rating
- **`duration`**: duration (in min for movies  and number of seasons (e.g., 2 Seasons) for shows)


<br><br>



- The following is the data.frame, **netflix**.
```{r}
rmarkdown::paged_table(netflix,
                       options = list(rows.print = 20,
                                      cols.print = 6,
                                      pages.print = 0,
                                      paged.print = F
                                      )) 
```


<br>


## Q3a.

- Separate the variable `imdb_score` into the two *numeric* variables, **imdb** and **imdb_max**, for which
  - (1) **imdb** is a value of **imdb_score** before `/`;
  - (2) **imdb_max** is a value of **imdb_score** after `/`.

- For example, if **imdb_score** == 6.6/10, then **imdb** == 6.6 and **imdb_max** == 10.

- Separate the variable `genre` into the following three variables, 
  - **genre_1**, **genre_2**, and **genre_3**, 
  - for which a value of each variable is one single genre.


- Separate the variable **production_country** into the eight variables,     - **country_1**, **country_2**, **country_3**, **country_4**, **country_5**, **country_6**, **country_7**, and **country_8**, 
  - in which a value of each variable is one single country.
  
  
- The following displays the variables in the resulting data.frame, `netflix_str_cleaned`:

```{r}
#| echo: true
netflix_str_cleaned <- read_csv(
  "https://bcdanl.github.io/data/netflix_str_cleaned.csv")

```

```{r, results = 'asis'}
rmarkdown::paged_table(netflix_str_cleaned) 
```


```{r}
netflix_str_cleaned <- netflix %>%
  separate(imdb_score, into = c("imdb", "imdb_max"), sep = "/", convert = TRUE) %>%
  separate(genre, into = c("genre_1", "genre_2", "genre_3"), sep = ",\\s*", fill = "right") %>%
  separate(production_country,
           into = c("country_1","country_2","country_3","country_4",
                    "country_5","country_6","country_7","country_8"),
           sep = ",\\s*", fill = "right")
```



<br><br>

## Q3b.

- Provide an R code to create the data.frame **q3b** with only the following two variables:
  - **country**: country that produced a show
  - **n_shows**: number of shows produced in a particular country.



- The following displays the variables in the resulting data.frame, `q3b`:

```{r}
q3b <- read_csv('https://bcdanl.github.io/data/netflix_country.csv')
```

```{r}
rmarkdown::paged_table(q3b) 
```

- Notice that, the observations are arranged by **n_shows** in descending order.


```{r}
q3b <- netflix_str_cleaned %>%
  pivot_longer(starts_with("country_"),
               names_to = "country_num",
               values_to = "country") %>%
  filter(!is.na(country)) %>%
  count(country, name = "n_shows", sort = TRUE)
```



<br><br>

---

# ✅ End of Homework 3

